import {React} from "react";
import "./style.css";

export const Frame88 = ({className}) => {
    return (
        <div className={`frame ${className}`}>
            <div className="group" />
        </div>
    );
}